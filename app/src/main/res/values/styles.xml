<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/SlideRightAnimation</item>
    </style>

    <style name="Fullscreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="MainActivityTheme" parent="AppTheme"/>

    <style name="AppThemeFit" parent="AppTheme"/>

    <style name="Transparent" parent="AppTheme"/>

    <style name="Switch" parent="AppTheme">
        <item name="colorControlActivated">#788EFA</item>
        <item name="colorSwitchThumbNormal">#BEBEBE</item>
        <item name="android:colorForeground">#BEBEBE</item>
    </style>

    <style name="PopupAnimation" parent="android:Animation" mce_bogus="1">
        <item name="android:windowEnterAnimation">@anim/popup_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_exit</item>
    </style>
</resources>